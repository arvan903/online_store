import 'package:flutter/material.dart';

class CAB extends StatelessWidget {
  const CAB({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Custom Action Bar'),
    );
  }
}
